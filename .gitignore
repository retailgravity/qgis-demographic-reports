# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.pyc

# QGIS compiled resources
# NOTE: resources.py is required at runtime (`from .resources import *`) and
# must stay tracked so the plugin installs from a fresh clone.
*_dialog_base.py

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# OS
Thumbs.db
desktop.ini

# Test files
test_data/
*.pdf
*.csv
*.gpkg

# Personal / per-install config (keep OUT of the public repo)
logo.png
# Pre-configured drive-time server for a specific install. The public build must
# ship WITHOUT this so it does not default every installer onto a shared server.
default_server.txt